import PageSkeleton from "@/components/PageSkeleton";

// Instant-loading placeholder for this route — see components/PageSkeleton.tsx.
export default function Loading() {
  return <PageSkeleton />;
}
