/**
 * Single source of truth for the version shown in Settings.
 * Bump this by hand whenever you ship a noticeable change. This is
 * independent from NEXT_PUBLIC_BUILD_ID (which is just a per-deploy
 * fingerprint used to detect stale client bundles).
 */
export const APP_VERSION = "1.0.0";
